@model MovieDatabase.Models.Genre

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Genre</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.Raw("Genre name")
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.tag)
        </dd>
        <dt class="col-sm-2">
            @Html.Raw("Movies")
        </dt>
        <dd class="col-sm-10">
            @foreach (Movie m in ViewBag.moviesVB)
            {
                @Html.Raw(m.title + ", ")
            }
        </dd>
    </dl>
</div>
<div>
    @if (ViewBag.isAdminVB)
    {
        <a asp-action="Edit" asp-route-id="@Model?.id" style="text-decoration: none">
            <button class="btn btn-secondary">Edit</button>
        </a>
    }
    <a asp-action="Index" style="text-decoration: none">
        <button class="btn btn-secondary">Genres list</button>
    </a>
</div>
